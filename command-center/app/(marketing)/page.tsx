import Observatory from "@/components/observatory/Observatory";

// Obsidian Observatory is now the primary interface (Portfolio · Markets · Trade
// · Advisor · Gov). It renders full-screen behind the password gate.
export default function Home() {
  return <Observatory />;
}
